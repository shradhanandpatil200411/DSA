let s = "anagram";
let t = "slient";

function Anagram(s, t) {
  let splitS = s.split("");
  let splitT = t.split("");

  let ObjectS = {};
  let ObjectT = {};
  for (const key in splitS) {
    if (!Object.hasOwn(object, key)) continue;

    const element = object[key];
  }
  {
    if (ObjectS.hasOwnProperty(splitS[i])) {
      ObjectS[splitS[i]] = 1 + 1;
    }
  }
  console.log(ObjectS);
}

Anagram(s, t);
